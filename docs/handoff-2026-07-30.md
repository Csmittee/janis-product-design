# Handoff — 2026-07-30, end of session

Read this first when opening a new chat. It's the complete, current
story — no need to re-read older chat log entries unless you want the
blow-by-blow of how a specific number was arrived at.

## Live files (branch `claude/bbq-lid-hinge-three-rib-0uxfpi`)

- **`BBQ-offset-smoker-base-v14.scad`** — the real active/current file.
  Includes `BBQ-understructure-v19.scad`, which includes
  `BBQ-chambers-v26.scad`. All 3 are the current live set.
- `BBQ-offset-smoker-base-v13.scad`/`v12.scad` kept in the folder as the
  last-3 historical versions, each frozen at its own real end-of-round
  state — do not edit either, historical record only.
- `BBQ-offset-smoker-base-v10.scad`/`v11.scad` archived to `archive/`
  today (aged out of the last-3-live window as v13/v14 were created).

**Governance file versions as of this handoff**: `PART_MANIFEST.md`
1.46, `SKELETON_WORKSHEET.md` 1.34, `knowledge.map` v102,
`rules-bbq-fab.md` 1.18. If you're reading this from a stale copy,
check these version numbers against the live files before trusting
anything else in this handoff.

## What's built and locked

**Rib profile** (`t1`..`t7u`, `T4U`, `t6be` — full labeled diagram:
`docs/rib-cb1-terminology.png`): t1 (handle bore) → t2/t3 (up the A-B
wall) → t4 (B-C/C-D miter) → T4U (perpendicular from apex C, the real
top-line anchor) → t5 → t6be/t7/t7u (CB1 bracket neck) → t6 (pivot
bore). This profile itself is locked — the only things that move are
CB1's own position/size, the shared pivot, the handle, and now the tray
hinge Z.

**CB1 is 2 real, separate parts**:
1. **The bracket** (`cb1_link_2d()` → `Ua`/`Ub`/`Uc`, one single
   polygon) — locked, wraps half of a 4" square tube on all 3 ribs.
2. **The counterweight pipe** (`cb1_pipe()`) — a real 4" square tube,
   both ends capped, real computed mass **~8.0kg** (matches Janis's own
   recollection and the old locked spec from `archive/BBQ-offset-
   smoker-base-v9.scad` almost exactly). This had been missing (never
   re-added as a mass) since v11 — restored this session. Its
   cross-section is built with the SAME `tangential_pt()`/
   `freeze_from_open()` frame the bracket uses, so it stays flush and
   aligned with the bracket's own slot (fixed a real angle bug this
   session — first pass had it axis-aligned instead of DE-tangent).

**4 parametric knobs** a future round can move without touching
anything else: CB1 position/size (`CB1_EDGE_FRAC`/`CB1_STANDOFF`/
`CB1_OD`), the shared pivot (`FC_Y`/`FC_Z` in `BBQ-chambers-v26.scad`),
the handle (`HANDLE_Y`/`HANDLE_Z`), and the tray hinge Z (`HINGE_Z`,
itself derived from the handle boss so it self-adjusts if the handle
ever moves).

**Prep trays** — relocated this session (`docs/tray-relocation-
bracket.md` has the full derivation + diagram) to clear the grab
handle, which they used to collide with:
- New triangular gusset bracket (`tray_bracket()`, 4 copies, apex A /
  hal / al) mounted on face HA (the existing chamfer wall below apex
  A — confirmed by Janis: the octagon's 8th vertex, going around the
  alphabet, is "H"; face HA is the face just before apex A).
- New `HINGE_Z` = lowest point of the handle boss minus 15mm.
- A folding support link: `ts`=`al` (the fixed anchor), `tt` found by
  projecting 45° from `al` up to the tray's own Z — **link length
  262.1mm**. Now **real, visible geometry** (`tray_link()`, 2 rod
  segments meeting at an elbow), shown at the tray's deployed (0°)
  reference position. Real, disclosed kinematic finding (checked via
  circle-circle intersection in Python, not assumed): a fixed-length
  2-bar link anchored at `al` genuinely cannot follow the tray through
  the full stow sweep — the `ts`-to-`tt` distance only grows as the
  tray folds up (`ts` sits low, `tt` swings away from it, not toward
  it) — matching why real folding-shelf hardware for this job commonly
  uses a slotted, not fixed, pivot at one end.
- A 10mm skirt (`TRAY_SKIRT_H`) on the tray's **tip and both left/right
  side edges** (moved off the hinge-side edge this round — the first
  pass visibly sank into the hinge block, a real defect Janis caught by
  eye since it's not a manifold error, just overlapping solid material).
  `HINGE_OUT` widened +15mm for real clearance around the link hardware.

## Moment analysis result (`docs/lid-hinge-moment-analysis.md`)

Real gravity moment 0-90°, computed from actual STL-derived mass/CG
data (not hand math). With the CB1 pipe restored:

| Janis's requirement | Target | Computed | Verdict |
|---|---|---|---|
| Startup lift at 0° | 3-7 kgf | 2.5 kgf | close, slightly under |
| Zero moment at/after 45° | crosses negative ≥45° | crosses at 89.2° | technically meets "or later," but very late |
| Gentle negative before 90° | negative before 90° | negative only in the last ~1° | marginal |
| Self-holding at 90° | ~5 kgf pull needed to close | -0.02 kgf (near-neutral) | fails |

**Open decision, not yet resolved**: the pipe's current position/size
(tuned for bracket clearance) lands the door almost exactly *balanced*
at full open rather than *self-holding*. Hitting the full target needs
more counterweight leverage — a heavier pipe, a longer `CB1_STANDOFF`,
or a different `CB1_EDGE_FRAC`. Not changed this session since Janis
called the bracket "locked" — this needs an explicit decision before
anyone moves CB1's position/size.

## Open items, not fixed, carried forward

1. **Rib0/rib2 vs tray X-span interference** — `RIB0_X`=200 sits inside
   tray0's own X-span, `RIB2_X`=715 sits inside tray1's — a real,
   CGAL-confirmed structural interference between the door-side rib arm
   and a stowed/mid-folded tray at `door_open_deg`=0. Unaffected by
   this session's tray Z relocation (it's an X-position conflict, not a
   Z-band one). Needs either an operating-sequence rule or relocating
   the rib X convention / tray hinge X spacing.
2. **CB1 counterweight tuning** (see above) — needs a decision on how
   much more leverage to add.
3. **Folding link mechanism** — real geometry now shown at the deployed
   reference position (length 262.1mm), but a fixed-length 2-bar link
   from this anchor point cannot kinematically follow the tray through
   the full stow sweep (see above) — real hardware for this job likely
   needs a slotted pivot at one end, not modeled here.

## Process note for whoever picks this up next

This session's own real process lapse, now fixed: several real rounds
of changes got made in place to a file whose name never changed,
instead of bumping to a new version file each time. The standing rule
is absolute — **any round with real geometry/logic changes (not a pure
pointer bump) gets a new version file**, every time. Bump first, then
edit.

# Handoff — 2026-07-31, end of session

Read this first when opening a new chat. Supersedes
`docs/handoff-2026-07-30.md` (kept, not deleted — real history of the
v15→v20 tray-link saga lives there if you want the blow-by-blow). This
doc covers what changed AFTER that handoff: PR #172 merged (v20
mechanism confirmed correct), a full sanitization/verification pass, 5
new subsystem construction-rule docs, and a full 0-90° moment/kgf sweep.

## Live files (branch `claude/bbq-lid-hinge-three-rib-0uxfpi`)

Unchanged from the 07-30 handoff — still `BBQ-offset-smoker-base-v20.scad`
→ `BBQ-understructure-v19.scad` → `BBQ-chambers-v26.scad`. No version
bump this round: this round touched governance docs, one stale comment
(see below), and added analysis/viewer work, not live geometry — no new
`.scad` version file was warranted (the standing "bump first, then
edit" rule applies to real geometry/logic changes, not doc/comment work).

**One real code fix, not geometry**: `BBQ-chambers-v26.scad`'s
`HINGE_PIVOT_Z` line carried a stale comment (`1445.335mm`) that never
matched the live computed value (`1345.34mm`, echo()-verified) — fixed
the comment only, the constant/formula itself was always correct. Found
while independently re-deriving the moment analysis fresh (see below).

## What's new this round

1. **SCAD cleanliness confirmed, not just assumed.** Ran a real dead-
   code sweep on `BBQ-offset-smoker-base-v20.scad` (every retired v19
   name — `TRAY_MOUNT_GAP`, `TRAY_LINK_X_SPLIT`, `tray_link_lc()`, etc.
   — confirmed genuinely absent, not just renamed) and an unused-top-
   level-variable sweep (134 constants checked; the only 3 single-
   reference hits — `AXLE_BORE_D`, `R_HANDLE`, `Ubbc` — are each an
   intentional named reference point with its own documenting comment,
   not dead code). File version set (`v18/v19/v20` base,
   `v24/v25/v26` chambers, `v17/v18/v19` understructure) already matches
   the last-3-live/archive convention — no archiving needed this round.
2. **5 new subsystem construction-rule docs**, one per major subsystem,
   written so a future chat/cc — on this product OR a future BBQ variant
   (round bullet capsule, square firebox, different scale, reverse
   offset) — can revise a subsystem without re-reading the full
   round-by-round history:
   - `bbq-offset-smoker/construction-rules-1-chamber.md`
   - `bbq-offset-smoker/construction-rules-2-firebox.md`
   - `bbq-offset-smoker/construction-rules-3-understructure.md`
   - `bbq-offset-smoker/construction-rules-4-ribs-counterbalance.md`
   - `bbq-offset-smoker/construction-rules-5-tray-system.md`

   Each states the subsystem's CONCEPT (its role, invariant across
   future variants), its generalizable RULES (pointing at
   `rules-bbq-fab.md`'s locked sections for full derivations rather than
   duplicating them — one source of truth per rule), the CURRENT live
   implementation anchors (file/module/constant names), and a
   new-variant checklist. Read the relevant one before starting work on
   that subsystem, on this product or the next one.
3. **Full 0-90° moment/kgf sweep**, replacing the prior 3-point (0°/45°/
   90°) table — `docs/lid-hinge-moment-analysis.md`, freshly re-derived
   (not copied) from live STL exports of the v20/v26 geometry, CB1 pipe
   included. Every mass/CG number cross-checked against the prior 3-point
   doc's own figures and matched to within STL faceting precision —
   confirms the v19→v20 tray-link rewrite (a different mechanism
   entirely) did not disturb the lid/rib/CB1 rigid body or its pivot.
   Headline numbers: **20.93kg total rotating mass, 2.46 kgf startup
   lift at 0°, turning point (positive→negative) at 89.4°, only 0.03 kgf
   holding force at full open** — i.e. still near-neutral equilibrium at
   90°, not Janis's stated ~5 kgf self-holding target. Same open tuning
   decision as before (CB1 position/size), now with per-degree data
   backing it instead of 3 sample points.

## What's built and locked (unchanged from 07-30, still current)

See `docs/handoff-2026-07-30.md`'s own "What's built and locked" section
for the rib profile (`t1`..`t7u`) and CB1's 2-part structure (bracket +
counterweight pipe) — nothing in that section changed this round.

## Open items, carried forward (unchanged unless noted)

1. **Rib0/rib2 vs tray X-span interference** — still open, not touched
   this round.
2. **CB1 counterweight tuning** — still open; now backed by the full
   sweep above instead of 3 points, same underlying decision needed.
3. **`ts`'s hinge shifted off the bracket's own symmetric face**
   (`TRAY_LINK_BRACKET_CLEAR`) — still open, not touched this round.
4. **Front wheels vs `front_bracket()`/`front_caster_plate()`/
   `tow_triangle()`** — ~6mm CGAL collision, documented in
   `SKELETON_WORKSHEET.md` PART A and now also in
   `construction-rules-3-understructure.md`, still open.

## Next up (per Janis's own stated priorities, not yet started)

1. **Viewer integration** — wire this product into
   `viewer/janis-product-viewer.html` using the same `modelsFolder`
   dynamic-picker pattern VM-01 already uses (see
   `.claude/SKILL_viewer_update.md`), plus a universal STL filename
   convention so Janis can freely export any door-open/closed/mid-fold
   state without a per-file cc prompt. Also fix the viewer's grid floor
   helper to sit at the model's real floor Z instead of crossing through
   the product's middle.
2. Janis's own future roadmap: a universal BBQ design-system prompt
   already drafted in `/docs` but explicitly not ready to launch yet —
   do not start a new product line from it without Janis's explicit go.

## Process note for whoever picks this up next

Same standing rule as the 07-30 handoff: any round with real geometry/
logic changes gets a new version file, bump first then edit. This
round's own governance-only work (docs + 1 stale comment fix) correctly
did NOT bump a version — don't over-apply the rule to non-geometry
rounds either.
